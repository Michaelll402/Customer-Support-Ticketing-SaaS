import { PageScaffold } from '@customer-support/ui';

const TicketsPage = () => {
  return (
    <PageScaffold
      eyebrow="APP SHELL"
      title="Tickets Placeholder"
      description="Ticket list, ticket detail, and create-ticket flows are not implemented in Milestone 0. This route exists to lock the future information architecture."
    />
  );
};

export default TicketsPage;
